#include "monty.h"

/**
 * f_nop - Doesn't do anything.
 * @head: Double pointer to the head of the stack.
 * @counter: The line number currently being run.
 *
 * Description: This function is designed to handle the 'nop' opcode.
 * The 'nop' opcode doesn't do anything.
 */
void f_nop(stack_t **head, unsigned int counter)
{
(void)head;
(void)counter;
}

